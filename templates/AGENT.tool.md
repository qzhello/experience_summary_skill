<!--
  tool 预设:CLI、构建脚本、运维工具、一次性数据处理等。
  导航重点:用法示例 / 边界条件 / 失败案例 / 退出码语义。
  log.md 高频 type:pitfall(用法) / note(边界)。
-->

# <项目名> · 工具项目经验

> AI 首读入口。本目录遵循:log.md 是 source of truth,本文件可重建。

---

## 1. 项目快照

- **一句话定位**: <TBD,如 "批量重命名 S3 对象的 CLI 工具">
- **技术栈**: <TBD>
- **入口**: <TBD,如 `bin/main.py`>
- **典型用法一行**: `<TBD>`

## 2. 适用版本 / 分支

- **当前版本**: <TBD,如 `tool@0.4.1`>
- **运行依赖**: <TBD,如 "Python 3.11 / aws-cli v2">

## 3. 用法 / 边界速查

> 最常被问"它怎么用"、"它什么时候会挂"。

- 标准用法示例: <一行命令>
- 必需环境变量: <TBD>
- 退出码语义: `0=ok / 1=usage / 2=runtime / 3=upstream`
- 已知边界: <如 "单次最多处理 10k 对象">

## 4. 当前 active 条目精选

> 只链 `status: active`。

### pitfall(用法陷阱、参数误区)
- [`<id>`](log.md#<id>) — <一句话>

### note(边界条件、失败模式、性能特征)
- [`<id>`](log.md#<id>) — <一句话>

### bug(已修复过的 bug,留作回归参考)
- [`<id>`](log.md#<id>) — <一句话>

### decision(为什么用这个实现 / 拒绝了什么)
- [`<id>`](log.md#<id>) — <一句话>

## 5. 录入规则

- 用法陷阱(参数顺序、隐式默认、容易看漏的开关) → `pitfall`。
- 工具的失败模式(网络断开会怎样、磁盘满会怎样) → `note`。
- 给出**最小可复现命令**比给抽象描述有用 —— 复现命令进 details 不进 log 正文。
- 其余规则见 skill 的 `conventions.md`。
